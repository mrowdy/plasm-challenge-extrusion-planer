[project]
name = "extrusion-planner"
version = "0.1.0"
description = "Look-ahead extrusion planning system for 3D printing with dynamic feed rate adjustment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
]
